export interface SelectedToken {
  address: string
  symbol: string
  name: string
  decimals: number
  balance: string
  amount: string
  logoUrl: string
}

export interface Token {
  address: string
  symbol: string
  name: string
  decimals: number
  balance: string
  logoUrl: string
}
